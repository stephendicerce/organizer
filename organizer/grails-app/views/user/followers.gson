import organizer.AuthToken
import organizer.User

response.status OK.value()
model {
    AuthToken token
    ArrayList<User> followers = []
}

json {
    status "success"
    data {
        token token

    }
}