import organizer.Organization

model {
    Organization organization
}

response.status OK.value()
json {
    status "success"
    data {
        accessToken : organization.admin.authToken.accessToken
        organization g.render(template: "templates/organization", model: [organization:organization])
    }
}